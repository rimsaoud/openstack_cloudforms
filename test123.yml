---
- name: test 123
  hosts: localhost
  tasks:
    - name: test1
      command: ip ad 
      register: ip
      become: true
      
    - debug: 
        msg: "{{ ip }}"
        
    - name: test2
      command: ls /root 
      register: ls
      become: true
      
    - debug: 
        msg: "{{ ls }}"
        
    - name: test3
      command: hostname
      register: host
      become: true
      
    - debug: 
        msg: "{{ host }}"
        
    - name: test21
      command: ls /root/ansible 
      register: ls
      become: true
      
    - debug: 
        msg: "{{ ls }}"
        
